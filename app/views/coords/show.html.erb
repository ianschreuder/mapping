<p>X UTM&nbsp;<%=h(@coord.x_utm)%></p>
<p>Y UTM&nbsp;<%=h(@coord.y_utm)%></p>
<p>Latitude&nbsp;<%=h(@coord.latitude)%></p>
<p>Longitude&nbsp;<%=h(@coord.longitude)%></p>

<%= link_to 'Edit', edit_coord_path(@coord) %> |
<%= link_to 'Back', coords_path %>